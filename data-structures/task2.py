#!/usr/bin/python3

# Replace
def replace_in_list(my_list, idx, new_value):
    if idx < 0 or >= len(my_list)
    return my_list

numbers = [10, 20, 30,40, 50,]
print("list:", numbers)

# replace an element at idx 3
list1 = replace_in_list(numbers, 3, 60)
print(list1)
print(replace_in_list(numbers, -3,70))
print(replace_in_list(numbers, 6, 90))
